<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lotusss.androidanimation">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".simpleDemo.ValueAnimatorActivity"></activity>
        <activity android:name=".sameAnim.CircleAnimationActivity" />
        <activity android:name=".sameAnim.CircleMotionActivity" />
        <activity android:name=".sharedElement.SharedElementTwoActivity" />
        <activity android:name=".sharedElement.SharedElementOneActivity" />
        <activity android:name=".simpleDemo.MotionActivity" />
        <activity android:name=".simpleDemo.ViewAnimActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>